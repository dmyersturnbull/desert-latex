%=======                    D E S E R T                    =======

\documentclass[
    feature / minted,      % ...................................... Minted for source code
    feature / imakeidx,    % ...................................... Index
    feature / tcolorbox,   % ...................................... Fancy boxes
    language = USenglish,        % ...............................  Language with dialect
    bib / hide-language = true,  % ................................ Hide e.g. "en" for language
    color / url = 0000D2,        % ................................ or named color, etc.
    cref / capitalize = true,    % ................................ E.g. "Figure 2"
    font-size / main = 11.8pt    % ................................ Decimal font size :)
    linespace / main = 1.6,      % ................................ 1.6 space
    numbering / footnotes = alph,  % .............................. Footnotes a, b, ...
    numbering / equation-within = chapter,  % ..................... Get "Equation [2.3]":
    numbering / equation-brackets = square-bold,
    numbering / theorem-like-within = chapter,
    style / foreign-words = {italic},  % .......................... Set text "styles":
    style / indx-starred = {xkcdalgaegreen,italic},  % ............ Affects \indx*
    style / nomenclature-group = {LARGE,uppertosc},  % ............ uppertosc: uppercase -> smallcaps
    glue / indent = 0em,  % ....................................... Set indent (fixed length)
    glue-between / paragraphs = 2ex plus 0.2ex minus 0.2ex,  % .... Space between paragraphs (rubber)
    glue-above / subsection = 2ex plus 0.2 ex minus 0.1ex    % .... Another true glue
    toc / depth = section  % ...................................... Max depth in TOC
]{desert}

% To modify labels:
\Translate{english}{
    algorithm = procedure,
    source code = computer code,
}
% Note: This is a wrapper around translation's \DeclareTranslation
%       e.g. \DeclareTranslation{english}{algorithm}{procedure}
%       Obviously we're abusing the intent a bit here :)

% Load extra packages:
\RequirePackage{pdfpages}  % to include a PDF

% Setup interfaces provided by various packages:
\SiSetup{
    group-minimum-digits=4,   
    group-digits=all,        
    group-separator=\thinspace,
}
% \GeometrySetup, \AcroSetup  \ChemSetup, \ChemfigSetup, \HyperSetup, \MintedSetup
% Note: These are just consistently named aliases; e.g. \AcroSetup is \acsetup

% Load bibliography and maybe some other things:
\LoadBib{REFS.bib}
\LoadTex{GLOSSARY.tex}
% Note: These are just aliases
%       \LoadBib is \addbibresource, and \LoadTex is \input

% Set document info
\SetInfo {
    subject = Physics,                     % some some mapped to XMP metadata
    title = Xenodynamic cloud computing,
    author = Karen Johnson,                % a comma-separated list
    degree = Doctor of Philosophy,         % arbitary extra data
    university = University of Washington,
    plaindate = 2022-03-22
}

\RenewDocumentCommand \TitleContents {} {
	``\info{title}"\\
	\Info{degree} \info{subject}[pre={in ``}, post={"}]\\
	Keywords: \infolist{keywords}[sep=|, style-inline={red;bold}]\\
	Authors: \infolist{author}[sep-{, }, sep-two={ and }, sep-last={ and }]
}

% More likely:
\RenewDocumentCommand \ShowTitle {} {
	\includepdf[pages=1]{university-titlepage.pdf}
}

\begin{document}
\LoadFrontDocs{sections/front}
\LoadMainDocs{sections/chapter1, sections/chapter2}
\LoadBackDocs{sections/back}
\end{document}
